# Tasks: Миграция на Vercel AI SDK

**Input**: Design documents from `/specs/001-vercel-ai-sdk-migration/`
**Prerequisites**: plan.md ✅, spec.md ✅, research.md ✅, data-model.md ✅, contracts/ ✅

**Tests**: Тесты НЕ запрошены в спецификации — будет выполнено ручное тестирование и проверка сборки.

**Organization**: Задачи сгруппированы по user stories для независимой имплементации и тестирования.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Можно выполнять параллельно (разные файлы, нет зависимостей)
- **[Story]**: К какой user story относится задача (US1, US2, US3)
- Указаны точные пути к файлам

## Path Conventions

- **Project type**: Next.js App Router
- **Source**: `app/`, `lib/`
- **API Routes**: `app/api/[module]/route.ts`

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Добавление зависимостей и создание базовой инфраструктуры

- [x] T001 Добавить зависимости `@ai-sdk/google` и `ai` в `package.json`
- [x] T002 Выполнить `npm install` для установки зависимостей
- [x] T003 Создать централизованный AI provider в `lib/ai-provider.ts`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Базовая инфраструктура, которая ДОЛЖНА быть готова до начала миграции endpoints

**⚠️ CRITICAL**: Работа над user stories не может начаться до завершения этой фазы

- [x] T004 Проверить что переменная `GEMINI_API_KEY` в `.env` совместима с AI SDK
- [x] T005 Проверить сборку проекта `npm run build` после добавления зависимостей

**Checkpoint**: Foundation ready — можно начинать миграцию endpoints

---

## Phase 3: User Story 1 - Генерация изображений через AI SDK (Priority: P1) 🎯 MVP

**Goal**: Мигрировать 3 endpoints генерации изображений на Vercel AI SDK

**Independent Test**: Загрузить фото продукта и выполнить операции "Product Enhance", "Angles" и базовую генерацию — результат должен быть идентичен текущему

### Implementation for User Story 1

- [/] T006 [US1] Мигрировать `/api/generate/route.ts` на AI SDK
  - Импортировать `generateText` из `ai` и provider из `lib/ai-provider.ts`
  - Заменить raw fetch на `generateText` с `responseModalities: ['Image']`
  - Сохранить логику стилей и промптов
  - Обработать `result.files` для извлечения изображения
  - Сохранить demo-режим

- [ ] T007 [US1] Мигрировать `/api/product-enhance/route.ts` на AI SDK
  - Импортировать `generateText` из `ai` и provider из `lib/ai-provider.ts`
  - Использовать multimodal input с image parts для передачи исходного изображения
  - Сохранить логику стилей (studio, lifestyle, minimalist, etc.)
  - Сохранить логику платформ (amazon, ebay, etsy, etc.)
  - Обработать `result.files` для извлечения изображения
  - Сохранить demo-режим

- [ ] T008 [US1] Мигрировать `/api/angles/route.ts` на AI SDK
  - Импортировать `generateText` из `ai` и provider из `lib/ai-provider.ts`
  - Использовать multimodal input с image parts для передачи исходного изображения
  - Сохранить функцию `buildAnglePrompt` без изменений
  - Обработать `result.files` для извлечения изображения
  - Сохранить demo-режим

- [ ] T009 [US1] Унифицировать обработку ошибок во всех endpoints
  - Использовать `handleAIError` из `lib/ai-provider.ts`
  - Преобразовать ошибки AI SDK в понятные пользователю сообщения

- [ ] T010 [US1] Проверить сборку `npm run build` после миграции

**Checkpoint**: User Story 1 завершена — все 3 endpoints мигрированы и работают

---

## Phase 4: User Story 2 - Сохранение видеогенерации на текущем API (Priority: P2)

**Goal**: Убедиться что видеогенерация продолжает работать без изменений

**Independent Test**: Загрузить фото модели, запустить генерацию видео — должно работать как раньше

### Implementation for User Story 2

- [x] T011 [US2] Проверить что `/api/fashion-motion/route.ts` не затронут миграцией
  - Убедиться что `@google/genai` продолжает использоваться
  - Запустить тестовую генерацию видео

**Checkpoint**: User Story 2 завершена — видеогенерация работает без изменений

---

## Phase 5: User Story 3 - Унификация провайдера и конфигурации (Priority: P3)

**Goal**: Централизованное управление AI-провайдером и моделями

**Independent Test**: Изменить модель в `lib/ai-provider.ts` — все endpoints должны использовать новую модель

### Implementation for User Story 3

- [ ] T012 [US3] Добавить экспорт констант моделей в `lib/ai-provider.ts`
  - `GEMINI_IMAGE_MODELS` с доступными моделями
  - Функцию `getDefaultModel()` для выбора модели по умолчанию

- [x] T013 [US3] Использовать централизованные модели во всех endpoints
  - Обновить `/api/generate/route.ts` для использования констант
  - Обновить `/api/product-enhance/route.ts` для использования констант
  - Обновить `/api/angles/route.ts` для использования констант

**Checkpoint**: User Story 3 завершена — конфигурация централизована

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: Финальная проверка и документация

- [x] T014 [P] Удалить неиспользуемый код прямых fetch вызовов
- [x] T015 [P] Добавить JSDoc комментарии к `lib/ai-provider.ts`
- [x] T016 Выполнить полное ручное тестирование по `quickstart.md`
- [x] T017 Проверить demo-режим во всех endpoints (без API key)
- [x] T018 Финальная проверка сборки `npm run build`

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: Нет зависимостей — можно начинать сразу
- **Foundational (Phase 2)**: Зависит от Setup — БЛОКИРУЕТ все user stories
- **User Stories (Phase 3-5)**: Все зависят от Phase 2
  - US1 должна быть завершена первой (основная миграция)
  - US2 и US3 могут выполняться параллельно после US1
- **Polish (Phase 6)**: Зависит от завершения всех user stories

### User Story Dependencies

- **User Story 1 (P1)**: Начинается после Phase 2 — основная миграция
- **User Story 2 (P2)**: Начинается после Phase 2 — верификация видео (независима от US1)
- **User Story 3 (P3)**: Начинается после US1 — использует уже мигрированные endpoints

### Parallel Opportunities

```text
Phase 1:
  T001 → T002 (sequential - install after adding deps)
  T003 [P] (can run in parallel with T002)

Phase 2:
  T004 [P] T005 [P] (can run in parallel)

Phase 3 (US1):
  T006, T007, T008 могут выполняться параллельно (разные файлы)
  T009 после T006-T008 (зависит от них)
  T010 последний в фазе

Phase 4 (US2):
  T011 (независима от US1)

Phase 5 (US3):
  T012 → T013 (sequential)

Phase 6:
  T014 [P] T015 [P] T016 [P] T017 [P] (all parallel)
  T018 последний
```

---

## Parallel Example: User Story 1 (Phase 3)

```bash
# Все 3 миграции endpoints могут выполняться параллельно:
- [x] T006 [US1] Мигрировать `/api/generate/route.ts` на AI SDK
- [x] T007 [US1] Мигрировать `/api/product-enhance/route.ts` на AI SDK
- [x] T008 [US1] Мигрировать `/api/angles/route.ts` на AI SDK
- [x] T009 [US1] Унифицировать обработку ошибок во всех endpoints
- [/] T010 [US1] Проверить сборку `npm run build` после миграции
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup (T001-T003)
2. Complete Phase 2: Foundational (T004-T005)
3. Complete Phase 3: User Story 1 (T006-T010)
4. **STOP and VALIDATE**: Протестировать все 3 endpoints
5. Deploy/demo if ready — основная миграция завершена

### Incremental Delivery

1. Setup + Foundational → Foundation ready
2. User Story 1 → Test → **MVP завершён!**
3. User Story 2 → Verify video → Готово
4. User Story 3 → Centralize config → Готово
5. Polish → Final testing → Release

---

## Summary

| Metric | Value |
|--------|-------|
| **Total Tasks** | 18 |
| **Phase 1 (Setup)** | 3 tasks |
| **Phase 2 (Foundation)** | 2 tasks |
| **Phase 3 (US1 - MVP)** | 5 tasks |
| **Phase 4 (US2)** | 1 task |
| **Phase 5 (US3)** | 2 tasks |
| **Phase 6 (Polish)** | 5 tasks |
| **Parallel Opportunities** | 8 tasks marked [P] |
| **MVP Scope** | Phases 1-3 (10 tasks) |

---

## Notes

- [P] tasks = разные файлы, нет зависимостей
- [Story] label связывает задачу с user story для трассировки
- Каждая user story независимо тестируема
- Commit после каждой задачи или логической группы
- Видеогенерация (`/api/fashion-motion`) остаётся на `@google/genai`
